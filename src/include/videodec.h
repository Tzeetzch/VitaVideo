/* Compatibility shim for the SCE SDK <videodec.h>. VitaSDK port.
 * SceAvPlayer pulls in the video codecs itself; this is kept for include
 * compatibility with the original sources. */
#ifndef VMP_VIDEODEC_SHIM_H
#define VMP_VIDEODEC_SHIM_H
#include <psp2/videodec.h>
#endif
